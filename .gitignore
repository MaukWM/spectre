# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
*.egg
dist/
build/
.eggs/

# Virtual environments
.venv/
venv/
env/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Environment
.env
.env.local

# OS
.DS_Store
Thumbs.db

# Testing
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/

# uv
.cache/

local_docs/
local_scripts/

# Local-only ROMs + Dolphin save states (licensed binaries, large)
roms/

# Inspect AI eval logs
runs/

# Per-sample derived artifacts (extracted from the ROM at build time)
samples/*/boot.dol
samples/*/boot.map

# Legacy per-sample Ghidra analysis cache (E2 layout; superseded by
# content-addressed cache/binaries/ under E3, still gitignored for
# back-compat).
samples/*/analysis/

# Content-addressed Ghidra analysis cache + ISO-extracted binaries.
# Machine-local, ~50 MB+ per binary; rebuilt on demand. Notes/renames
# live inside `cache/binaries/<sha1>/notes.json` (mutable per binary).
cache/